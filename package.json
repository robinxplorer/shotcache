{
  "name": "shotcache",
  "version": "0.2.0",
  "private": true,
  "description": "Shotcache (截存): capture, annotate, stash for a few days, send to Claude/ChatGPT/Gemini in one click.",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "compile": "tsc --noEmit",
    "postinstall": "wxt prepare"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "wxt": "^0.20.0"
  }
}
